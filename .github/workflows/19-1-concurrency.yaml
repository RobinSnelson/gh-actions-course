name: 19 - 1 - Managing Concurrency
on:
    workflow_dispatch: 
concurrency:
    group: <retrieve the workflow name here>-<retrieve the git ref here>
jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/Checkout@v4 
            - name: Ping URL
              uses: ./.github/actions/docker-ping-url
              with:
                url: http://vcswivcwivc.co.uk
                max_trials: 20
                delay: 5